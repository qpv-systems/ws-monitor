# ============================================
# WebSocket Proxy Monitor - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# cp env.example .env

# ============================================
# Server Configuration
# ============================================
# Port để chạy proxy server
# Default: 8000
PORT=8000

# Node environment
# Options: development, production, test
# Default: production
NODE_ENV=production

# Giới hạn số lượng client kết nối đồng thời
# Khi vượt quá giới hạn, connection mới sẽ bị từ chối
# Default: 0 (không giới hạn)
# Ví dụ: MAX_CLIENTS=10 (cho phép tối đa 10 connections)
MAX_CLIENTS=0

# ============================================
# Optional: CORS Configuration (nếu cần customize)
# ============================================
# CORS origin cho Socket.IO
# Default: "*" (cho phép tất cả)
# CORS_ORIGIN=*

# ============================================
# Optional: Logging
# ============================================
# Log level
# Options: debug, info, warn, error
# LOG_LEVEL=info

# ============================================
# Docker Hub (cho CI/CD)
# ============================================
# Không cần set trong .env, được quản lý bởi GitHub Actions
# DOCKERHUB_USERNAME và DOCKERHUB_TOKEN được lưu trong GitHub Secrets
# 
# Để pull image từ Docker Hub thủ công:
# docker login -u <your-dockerhub-username>
# docker pull <your-dockerhub-username>/ws-monitor:latest

# ============================================
# Production Secrets
# ============================================
# Các secrets này được quản lý qua:
# 1. GitHub Secrets (cho CI/CD)
# 2. Docker secrets (cho production)
# 3. Environment variables trên server

# Ví dụ các secrets có thể cần (nếu mở rộng sau):
# API_KEY=your-api-key-here
# DATABASE_URL=postgresql://user:pass@host:5432/db
# REDIS_URL=redis://localhost:6379

